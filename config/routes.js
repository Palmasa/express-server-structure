const router = require('express').Router()

// Require controllers

// Require middlewares
    // session secure/guard user/no-user
    
// Multer to upload files
    // const multer = require('multer')
    // const upload = multer({ dest: './public/uploads/' });

// Routes
